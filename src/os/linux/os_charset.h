
#ifndef _OS_CHARSET_H
#define _OS_CHARSET_H

// #include <stdio.h>
#include <stdlib.h>
#include <iconv.h>

// void sis_gbk_to_utf8(const char*  in, char*  out, size_t  olen_);
// void sis_utf8_to_gbk(const char*  in, size_t  ilen_, char*  out, size_t  olen_);

// void sis_get_spell_utf8(const char *in, char *out, size_t);
// void sis_get_spell_gbk(const char *in, char *out, size_t);

#endif //_SIS_LOG_H